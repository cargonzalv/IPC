export const FONT_SIZES: Record<string, number> = {
  baseline: 14,
  compact: 12,
};

export const HEIGHT_SIZES: Record<string, number> = {
  baseline: 40,
  compact: 12,
};

export const INPUT_TYPES: Record<string, number> = {
  basic: 4,
  rounded: 20,
};
