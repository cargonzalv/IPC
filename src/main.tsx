import ReactDOM from 'react-dom/client';
import { PlaygroundContainer } from './app/features/playground/components/container';

ReactDOM.createRoot(document.getElementById('root')!).render(<PlaygroundContainer />);
